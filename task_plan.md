# P0/P1/P2 架构改造执行计划

## 目标

按 `ARCHITECTURE_REVIEW.md` 推进架构改造。P0、P1-1、P1-3 已完成；当前进入 P2，先拆后端协调器，再实现持久事件补发，保持热重载注册表、消息正文不持久化和现有重连保护。

## P0 阶段

- [完成] P0-0：核对调用点、既有测试风格与修改边界
- [完成] P0-1：新增通用 JSON 请求工具及测试，迁移三个优先组件
- [完成] P0-2：为 application/services 增加架构边界守卫和唯一例外白名单
- [完成] P0-3：抽取会话树、斜杠排序、模型兼容转换并补测试
- [完成] P0-4：运行相关测试、全量测试、类型检查和 lint，并同步架构报告与项目规则

## P1 阶段

- [完成] P1-1a：盘点 `useAgentSession` 状态、回调和依赖关系，确定第一批低耦合边界
- [完成] P1-1b：抽取通知队列职责并补行为测试
- [完成] P1-1c：抽取扩展界面状态职责并补行为测试
- [完成] P1-1d：抽取模型与工具配置职责并补行为测试
- [完成] P1-1e：核对对外返回契约和调用方，删除已迁出的中转代码
- [完成] P1-1f：全量测试、类型检查、lint 与文档收口
- [完成（后续解阻）] P1-2：用户选择 Zustand 后完成共享导航/工作区状态迁移
- [完成] P1-3a：盘点 ModelsConfig、ChatInput、SessionSidebar 的可搬迁边界
- [完成] P1-3b：拆 ModelsConfig 授权/密钥/连接测试子面板
- [完成] P1-3c：拆 ChatInput 命令面板/附件/模型选择子面板
- [完成] P1-3d：拆 SessionSidebar 工作树区域
- [完成] P1-3e：全量验证与文档收口

## P1-1 成功标准

- `useAgentSession` 不再直接维护通知队列、扩展界面、模型与工具配置三类内部实现。
- 每个新钩子只接收完成本职工作所需的最小输入，并有可独立运行的行为测试或纯状态测试。
- 主钩子对调用方的返回字段和行为保持兼容；SSE、轮询、可见性对账、运行编号守卫不改语义。
- `npm test`、`npm run typecheck`、`npm run lint` 全部通过；不运行 `next build`。

## P2 阶段

- [完成] P2-0：复核协调器、事件 journal、SSE 路由和现有测试基线
- [完成] P2-1a：盘点 `rpc-manager.ts` 的公开接口、全局注册表、生命周期、扩展界面和命令分派边界
- [完成] P2-1b：抽取全局注册表/启动锁与生命周期辅助模块，保留 `globalThis` 热重载语义
- [完成] P2-1c：按职责抽取扩展界面上下文和命令处理器，缩小主协调器
- [完成] P2-1d：补协调器边界/行为测试并全量验证
- [完成] P2-2a：定义持久事件补发契约，只补 journal 事件，消息正文仍靠状态快照
- [完成] P2-2b：给服务端 SSE 增加单调序号和 `since` 补发，补服务端测试
- [完成] P2-2c：客户端记录已处理序号，重连时请求补发并保留旧运行迟到守卫
- [完成] P2-2d：补发链路自动化通过；运行状态 SSE 真实断线重连通过，活跃生成补发仍保留轮询兜底
- [完成（后续解阻）] P2-3：Zustand 接管共享导航/工作区状态，删除 5 个中转参数
- [完成] P2-4：全量验证、架构报告、项目规则和进度收口

## P2 成功标准

- `rpc-manager.ts` 不再直接承载全部注册、扩展界面和 25 类命令实现；现有导出与调用方保持兼容。
- `globalThis.__piSessions` 与启动锁语义、fork 后销毁、10 分钟空闲回收不变，并有测试锁定。
- SSE 可按单调序号补发已持久化的运行/操作事件；不持久化流式 token 或消息正文。
- 客户端旧运行结果不能覆盖新运行；在没有端到端证据前不删除 15 秒轮询。
- `npm test`、`npm run typecheck`、`npm run lint`、`git diff --check` 全部通过；除获批 Zustand 外不安装依赖，不运行 `next build`。

## P1-2 / P2-3 解阻执行

- [完成] S0：按用户裁决复核共享状态边界、调用方和 Zustand 当前用法
- [完成] S1：安装 Zustand，建立最小共享 store 与纯行为测试
- [完成] S2：迁移选中会话、新会话目录和有效工作区，保留侧栏工作树快照、未读集合、表单和弹窗本地状态
- [完成] S3：删除迁移后不再需要的中转 props、回调和兼容包装
- [完成] S4：删除 `lib/subagent/` 空目录，关闭 doctor/Node crypto/正文逐片段回放裁决项
- [完成] S5：全量自动化验证
- [完成] S6：按 webapp-testing 流程启动本地服务，执行浏览器焦点、弹层、工作树只读展示和断线恢复验证
- [完成] S7：同步报告、项目规则、进度和待裁决清单

## P1-2 / P2-3 成功标准

- Zustand 只承载至少两个顶层消费者共享的导航/工作区状态；表单输入、模态框开关和消息流状态仍留本地。
- AppShell、SessionSidebar、ChatWindow 不再通过长 props 链中转已迁入 store 的状态。
- 共享状态变更有纯行为测试；现有消息 SSE、运行编号守卫和 15 秒轮询语义不变。
- 浏览器验证覆盖主界面加载、会话/工作目录切换、关键弹层、控制台错误和一次真实断线恢复。
- `npm test`、`npm run typecheck`、`npm run lint`、`git diff --check` 全部通过；不运行 `next build`。

## 决策

- P1 先做报告明确推荐的 P1-1；P1-2 因依赖选择未裁决而跳过，继续推进不受影响的 P1-1/P1-3。
- 第一批只迁出不依赖 SSE 主链路的职责；不在同一批重写消息状态机。
- P1-3 只移动独立面板和局部交互状态；跨组件共享状态继续等待 P1-2 裁决。
- P2-1 先按“搬迁职责、保持公开入口”的方式拆协调器，不同时改运行时协议。
- P2-2 采用 `BLOCKED.md` 第 5 项中的保守范围：只补发 journal 已持久化事件，正文用当前状态恢复；不扩大敏感数据持久化。
- P2-3 因 P1-2 未裁决而跳过，不用后端改造反向替用户决定前端状态方案。
- 用户已于 2026-07-28 选择状态库并授权安装依赖与自行启动服务做浏览器验证；采用报告候选 Zustand v5，使用单字段 selector 和不可变更新控制重渲染。
- 不做流式正文逐片段回放；doctor 直连 persistence 和 application 的 Node crypto 维持现状；删除 `lib/subagent/` 本地空目录。
- 除已获批的 `zustand@5.0.12` 外不安装依赖，不运行 `next build`。
- 通用请求工具保留 HTTP 状态和解析后的错误响应体；三个优先组件的 25 处 fetch 全部迁移，OAuth EventSource 保留。
- 会话树循环节点安全降级为根节点，避免异常父子数据让会话从侧栏消失。

## 2026-07-28 上游版本差异调研

### 目标

以当前工作区 `d4f13d9`（应用版本 `0.8.2`）为基线，确认 `agegr/pi-web` 当前最新正式版本，并把本地分叉与该版本逐项比较，判断本项目需要跟进的代码、依赖、桌面发布和文档变更；本轮只读分析，不改业务代码。

### 阶段

- [完成] U1：确认上游最新正式版本、标签与发布说明
- [完成] U2：取得本地当前提交与上游 `v0.8.2` 的提交和文件差异
- [完成] U3：把上游差异映射到本项目当前代码，识别已自行实现、存在冲突和确需移植的内容
- [完成] U4：按优先级输出跟进清单、风险和建议验证方式

### 成功标准

- 最新版本、发布时间、提交范围均可由 GitHub 或 Git 对象复核。
- 每项建议至少有一条上游证据和一条本地现状证据。
- 明确区分“必须跟进”“建议跟进”“无需跟进或需人工裁决”。
- 不运行 `next build`，不修改业务代码，不提交、不推送、不发布。

## 2026-07-28 上游 v0.8.2 跟进实施

### 目标

按用户指定顺序，把已确认缺失的上游 `v0.8.2` 变更适配到当前分叉；保留通用内核、持久任务、MCP、桌面端、共享工作区状态和现有发布规则。

### 阶段

- [完成] S1：请求主机安全、关键写接口防护、项目资源信任门禁与受限模式
- [完成] S2：跨项目文件标签清理、同仓工作树保留、全局技能开关
- [完成] S3：快速变更总览、增删统计、差异模式与删除文件差异
- [完成] S4：底部固定扩展状态栏
- [完成] S5：中英国际化、语言识别与菜单、Shift 点击直删
- [完成] S6：全量测试、类型检查、lint、差异卫生、浏览器冒烟和文档收口

### 成功标准

- 恶意主机头、跨来源请求、缺失或畸形主机头被拒绝；本地监听地址、显式白名单、IPv4/IPv6 和既有局域网令牌仍正常。
- 未信任项目的扩展、技能和插件不加载、不允许修改；信任后刷新资源且不打断运行中的会话。
- 跨项目清理文件标签，同项目不同工作树保留；全局技能符号链接可正常切换。
- 文件变更总览可显示数量和增删统计，含已删除文件差异，并能直接以差异模式打开。
- 扩展状态固定在输入区底部；界面可在英文和简体中文之间切换；只有 Shift 点击删除按钮才跳过确认。
- `npm test`、`npm run typecheck`、`npm run lint`、`git diff --check` 全部通过；不运行 `next build`，不提交、不推送、不发布。

## 错误记录

- `planning-with-files` 的完成检查脚本只识别模板中的英文复选框，本项目计划沿用中文状态格式，因此报告 `0/0 phases`；人工核对 U1–U4 均为完成，且不影响差异分析。
- 本轮第一次同步调研记录时，多文件补丁的一段上下文匹配失败，未产生任何修改；读取实际段落后拆成小补丁重试。
- 全量测试存在项目原有的 `MODULE_TYPELESS_PACKAGE_JSON` 告警；188 项测试仍全部通过，本轮未改 package 配置。
- P1 第一次运行通知队列测试时，1/6 失败：测试误以为 reducer 在已有退场项时会忽略 `mark_oldest_exiting`；原实现实际会标记下一条，避免重复触发的是 hook 定时 effect。已修正测试预期，未改业务行为。
- 浏览器冒烟探测失败：本机 30141 端口没有开发服务；按项目规则未启动或重启服务，记录为运行态未验证。
- P1-3 最终旧状态扫描第一次使用双引号包住含反引号的搜索词，shell 尝试执行其中的 `useState` 并报 `command not found`；该命令只读、未改文件，随后改用安全引号重跑。
- P2-1 扩展界面桥首次接线补丁因 import 上下文与当前文件不完全匹配而被 `apply_patch` 拒绝；没有产生部分修改，改为读取窄范围后分小块接线。
- P2-1 断点记录第一次追加到 `findings.md` 时假定了不存在的 `## P2 取证` 标题，补丁被拒且未产生修改；读取实际尾部后改为追加到 `## 2026-07-28 P2 实施`。
- P2-2 第一次运行重连静态测试时，测试从整份路由源码比较字符串位置，顶部 import 让补发函数名早于订阅调用，产生 1 项误报；已把比较范围收窄到 `ReadableStream` 主体，业务代码未改。
- P1-2 首次调用关系扫描包含不存在的 `components/ChatWorkspaceView.tsx`，`rg` 报路径不存在；实际文件位于 `components/workspace/ChatWorkspaceView.tsx`，其余扫描结果有效，后续使用真实路径。
- P1-2 首次局部 lint 报 AppShell 两个回调缺少 Zustand action 依赖（2 warnings、0 errors）；action 引用稳定，但为满足 hook 规则已显式补入依赖数组。
- P1-2 首次共享状态静态测试扫了整个侧栏文件，把递归会话树内部的选中编号参数误判成顶层中转 prop（1/12 失败）；已将断言收窄到组件公开 `Props` 声明，业务代码未改。
- 删除空目录前的首次引用扫描包含不存在的 `scripts/` 路径，`rg` 报路径不存在；移除该路径后重跑，源码引用与目录内容均为 0。
- 首次浏览器验证时本地服务成功启动，但 Python Playwright 对应的 Chromium 可执行文件未安装，页面脚本未执行；服务已由测试助手自动关闭，后续优先复用本机现有 Chrome，不下载新浏览器。
- 复用本机 Chrome 后，测试助手报告 30141 就绪，但浏览器首次导航收到 `ERR_CONNECTION_REFUSED`；助手随后正常清理进程。需核对项目启动脚本的监听地址与子进程生命周期，不归因于页面代码。
- 手工读取开发服务日志确认真实原因：默认本地 kernel 数据库 schema 为 4，而当前工作区只支持 3，instrumentation 在就绪后立即退出。禁止改动或降级用户数据库；浏览器验证改用独立临时 `PI_WEB_DATA_DIR`。
- 临时数据目录下页面已加载，但 Next.js 开发错误覆盖层拦截了弹层按钮，浏览器脚本在首次模型按钮点击超时；服务已自动关闭。需读取覆盖层/服务端日志确认是否为本轮状态迁移引起的运行错误。
- 覆盖层探针确认页面脚本错误、控制台错误均为 0；实际是 Next.js 开发工具浮层与侧栏底部模型按钮重叠。冒烟脚本只隐藏开发工具 portal 后继续，不修改产品页面。
- 浏览器离线 1.5 秒后，既有 SSE TCP 连接在 12 秒内未被判死，因此没有出现新请求；页面其余交互已完成。该结果不能证明产品不重连，改用服务端真实关闭连接再恢复的方式验证，禁止用页面刷新替代。
- 服务端真实关闭并恢复后脚本已越过重连断言；最终失败来自弹层快速关闭主动取消的 3 个模型请求（`ERR_ABORTED`）。将主动取消单列为正常诊断后完整复跑，不降低其他网络错误断言。
- 第二次服务端重启验证再次越过重连断言，但项目选择器按“标题以绝对路径开头”定位未命中；页面仍正常。改按可见路径文字/实际按钮结构定位，避免把脚本假设当成产品故障。
- 可见路径的正则定位被 Playwright Python 错误转义为无效选择器，脚本在交互前退出；同一定位方向已连续失败 3 次，按止损规则改为普通文字过滤，不再使用路径正则。
- 源码复核确认“过滤项目”只在项目数量超过阈值时渲染，前述项目下拉断言本身无效；最终改为验证始终存在的“使用默认目录/自定义路径”入口，本轮后不再增加浏览器验证轮次。

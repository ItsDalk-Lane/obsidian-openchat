export const FETCH_WEBPAGES_BATCH_DESCRIPTION = `批量抓取多个已知网页并返回结果数组。

## 何时使用

- 已经有多个明确 URL，需要批量读取正文时
- 需要用单次工具调用比较多篇网页内容时
- 允许部分网页失败并保留逐项结果时

## 何时不使用

- **不要用于抓取单个网页**：单 URL 请使用 \`fetch_webpage\`
- **不要用于搜索未知网页**：需要先找网页时请使用 \`bing_search\`
- **不要用于读取 Vault 本地文件**：本地文件请使用 \`read_file\`

## 可用字段

- **urls**（必需）：多个目标网址组成的数组
- **max_length**（可选，默认 5000）：每个网页单次最多返回的字符数
- **start_index**（可选，默认 0）：每个网页从第几个字符开始返回
- **raw**（可选，默认 false）：是否跳过 HTML 提取和 Markdown 转换，直接返回原始内容

## 返回值

返回 JSON 数组。每个元素包含对应 URL、是否成功、内容或错误信息。

## 失败恢复

- 如果只有个别 URL 失败，可直接读取结果中的错误字段并重试对应网址
- 如果全部失败，先检查网络、robots 或黑名单限制
- 如果内容过长，结合 \`start_index\` 与 \`max_length\` 对目标网页分页抓取
`;

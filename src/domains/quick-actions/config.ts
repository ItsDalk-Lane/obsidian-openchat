/**
 * @module quick-actions/config
 * @description 定义 quick-actions 域的常量与默认值。
 *
 * @dependencies src/domains/quick-actions/types
 * @side-effects 无
 * @invariants 不包含运行时逻辑，不依赖 Obsidian API 或宿主能力。
 */

/** Vault 内存放快捷操作 Markdown 文件的子目录名。 */
export const QUICK_ACTIONS_SUBFOLDER = 'quick-actions';

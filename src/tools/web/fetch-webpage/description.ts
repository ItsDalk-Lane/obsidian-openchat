export const FETCH_WEBPAGE_DESCRIPTION = `抓取单个已知网页并返回正文内容。

## 何时使用

- 已经有明确的网页 URL，需要读取正文时
- 需要分页抓取较长网页内容时
- 需要把网页正文转换成更适合模型消费的 Markdown 时

## 何时不使用

- **不要用于搜索未知网页**：需要先找网页时请使用 \`bing_search\`
- **不要用于批量抓取多个网址**：多个 URL 请使用 \`fetch_webpages_batch\`
- **不要用于读取 Vault 本地文件**：本地文件请使用 \`read_file\`

## 可用字段

- **url**（必需）：目标网址，必须是 \`http\` 或 \`https\`
- **max_length**（可选，默认 5000）：单次最多返回的字符数
- **start_index**（可选，默认 0）：从第几个字符开始返回，用于分页读取长内容
- **raw**（可选，默认 false）：是否跳过 HTML 提取和 Markdown 转换，直接返回原始内容

## 返回值

返回处理后的网页内容文本；内容较长时会提示下一次继续读取应使用的 \`start_index\`。

## 失败恢复

- 如果 URL 格式错误，先修正协议和地址
- 如果正文提取失败，尝试设置 \`raw=true\` 获取原始内容
- 如果内容过长，使用返回提示中的下一段 \`start_index\` 继续读取
`;
